# 2026-04-28

## JavaScript Harness template setup

- Changed: set up the Harness template for JavaScript/TypeScript projects.
- Verified: `python Harness/scripts/verify_project.py` passed. JSON config parsing passed. `build_verify.ps1 -Mode Check` failed as expected because this standalone template root has no target `package.json`.
- Remaining: after migration into a target JavaScript project, run `build_verify.cmd` against the real `package.json` scripts.

## goose/ollama 역할 정리

- 변경: `ollama`를 독립 작업자가 아니라 goose가 사용하는 로컬 LLM 런타임으로 정리했다. `OLLAMA.md`는 제거 대상이 되었고, goose 지시와 설정에 모델 백엔드 설명만 남겼다.
- 검증: 설정 JSON 파싱 통과. `python Harness/scripts/verify_project.py` 통과.
- 남은 것: 대상 JavaScript 프로젝트에 이식한 뒤 goose의 실제 ollama 모델 설정은 로컬 환경에서 별도 지정 필요

## Repository baseline

- Changed: aligned git `origin` and Harness state with `https://github.com/rmsgh0911/Harness_Javascript.git`.
- Verified: checked fetch/push URL with `git remote -v`.
- Remaining: none.

## Primary worker alignment

- Changed: changed the default worker to `current_app` and allowed Codex and Claude Code as primary workers. Removed goose/ollama-centered wording.
- Verified: JSON config parsing passed. `git diff --check` passed.
- Remaining: none.

## Agent-facing English alignment

- Changed: aligned root agent files, Harness docs policy, and tool policy with the newer template convention: agent-facing Harness files are English, while `Harness/docs/Progress.md` remains Korean for humans.
- Verified: `harness_doctor.py`, `harness_verify_all.py`, `verify_project.py`, JSON parsing, `harness_diff_guard.py`, and `git diff --check` passed. `verify_project.py` reports expected standalone-template warnings because no target `package.json` exists here.
- Remaining: none.
